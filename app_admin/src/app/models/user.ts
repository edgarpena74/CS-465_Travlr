export class User {
  // User email address
  email: string;
  // User name
  name: string;

  constructor() {
    // Initialize email as empty string
    this.email = '';
    // Initialize name as empty string
    this.name = '';
  }
}
